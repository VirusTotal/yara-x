syntax = "proto2";

import "yara.proto";

option (yara.module_options) = {
  name : "magic"
  root_message: "Magic"
  rust_module: "magic"
};

message Magic {
  // This module contains only exported functions, and doesn't return any data
}
