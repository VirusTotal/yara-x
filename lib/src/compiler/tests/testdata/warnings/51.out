warning[unintended_pattern_in_set]: pattern `$start_bytes` may be unintendedly or redundantly included in pattern set `$s*`
 --> line:8:6
  |
8 |     ($start_bytes or $s1) and (any of ($s*))
  |      ------------                      --- `$start_bytes` is also included in `$s*`
  |      |
  |      `$start_bytes` is used here
warning[unintended_pattern_in_set]: pattern `$s1` may be unintendedly or redundantly included in pattern set `$s*`
 --> line:8:22
  |
8 |     ($start_bytes or $s1) and (any of ($s*))
  |                      ---               --- `$s1` is also included in `$s*`
  |                      |
  |                      `$s1` is used here
