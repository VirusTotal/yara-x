rule prefix_wildcard_outlier {
  strings:
    $prefix_1 = "foo"
    $prefix_2 = "bar"
    $prefix_3 = "baz"
    $prefix_unrelated_long_suffix = "test"
  condition:
    $prefix_unrelated_long_suffix at 0 and any of ($prefix_*)
}
