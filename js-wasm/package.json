{
  "name": "yara-wasm",
  "version": "0.1.0",
  "description": "WebAssembly build of yara-x with browser-friendly JavaScript APIs.",
  "type": "module",
  "sideEffects": false,
  "main": "./pkg/yara_wasm.js",
  "module": "./pkg/yara_wasm.js",
  "types": "./pkg/yara_wasm.d.ts",
  "exports": {
    ".": {
      "types": "./pkg/yara_wasm.d.ts",
      "import": "./pkg/yara_wasm.js",
      "default": "./pkg/yara_wasm.js"
    },
    "./bundle": "./dist/yara_wasm_bundle.min.js",
    "./bundle-dev": "./dist/yara_wasm_bundle.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist/**/*",
    "pkg/**/*",
    "README.md"
  ],
  "scripts": {
    "build:web": "cargo build-web-release",
    "prepack": "npm run build:web",
    "test:wasm-node": "wasm-pack test --node --mode no-install --test wasm_api && wasm-pack test --node --mode no-install --test wasm_modules",
    "test:wasm-browser": "wasm-pack test --headless --chrome --mode no-install --test wasm_browser_api",
    "test:wasm": "npm run test:wasm-node && npm run test:wasm-browser",
    "test:js": "node --test tests-js/*.test.js",
    "pack:dry-run": "npm pack --dry-run"
  },
  "keywords": [
    "yara",
    "yara-x",
    "wasm",
    "webassembly",
    "malware"
  ]
}
